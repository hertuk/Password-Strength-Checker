<?php

$password = "Test1234";

$strength = "Weak";

if (
    strlen($password) >= 8 &&
    preg_match('/[A-Z]/', $password) &&
    preg_match('/[0-9]/', $password)
) {
    $strength = "Strong";
}

echo "Password strength: " . $strength;

?>
